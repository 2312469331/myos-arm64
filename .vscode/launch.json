{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "QEMU ARM64 内核调试",
            "type": "lldb",
            "request": "attach",
            "targetCreateCommands": [
                "target create ${workspaceFolder}/build/kernel.elf"
            ],
            "processCreateCommands": [
                "gdb-remote localhost:1234"
            ],
            "stopOnEntry": true
        }
    ]
}